## v0.4.2

- 検証用の `smc-smoke-*` 一時フォルダが保存先表示へ残った場合、通常起動時にユーザーの既定Libraryへ安全に自動修復するようにしました。
- 自動修復はSNS Media Collector自身の既知の検証用パスだけを対象とし、通常のTempフォルダや実ファイルには触れません。
- 最近のダウンロードで長いファイル名がカード幅を崩さないよう中央を省略し、カーソルを置くと完全なファイル名を確認できるようにしました。
- 最後に保存したファイルと下部ステータスはファイル名だけを簡潔に表示し、完全なパスはツールチップへ移しました。

## v0.4.1

- 日常的に使う「取得メニュー」を左側の主役としてまとめ、選択中の設定名・取得元・認証アカウント・保存方針を中央上部にも表示するようにしました。
- ログイン管理を折りたたみ式に変更し、認証更新やアカウント編集が必要なときだけ展開できるようにしました。
- 背景・境界線・選択色・見出し・ボタンの優先順位を整理し、選択中の項目と次に行う操作を見分けやすくしました。
- 処理キューが空のときは待機状態を明示し、取得開始後だけ進行状況を表示するようにしました。
- 中央領域を広げ、右側の処理状況を適正な幅へ調整しました。

## v0.4.0

- 認証アカウントと「取得設定」を分離し、同じログインを用途・保存先・取得対象の異なる複数設定から再利用できるようにしました。
- 取得設定は左側でドラッグ＆ドロップして並び替えでき、選択だけでアカウント・取得元・範囲・保存先・保存方針をまとめて切り替えます。
- Xブックマークを追加しました。本文とメディア情報を先に軽く確認し、画像のみ・本文＋画像・本文のみを固定方針または確認箱で投稿ごとに振り分けられます。
- Xのアプリ内ログインCookieから秘密値を表示せず安定ユーザーIDを自動認識し、自分の投稿・メディア・いいねではユーザー名やURLの二重入力を不要にしました。
- 本文は元投稿・投稿日時・ブックマーク日時を含むMarkdownとして保存し、未振り分け投稿と選択結果はSQLiteへ永続化します。
- v0.3系のアカウント別作業設定から取得設定を初回起動時に自動作成し、認証・archive・アンカー・保存先を維持します。

## v0.3.13

- 取得対象と取得範囲をピル型の選択肢へ変更し、選択中を明るい青・太字・強い枠で表示します。
- アカウント一覧、チェック項目、プルダウン、詳細設定でも選択中の色とコントラストを統一しました。
- hover・フォーカス・選択中を別々の表示にし、現在の設定をひと目で判別しやすくしました。

## v0.3.12

- デスクトップとスタートメニューのショートカットを固定ランチャーへ変更し、更新後も同じショートカットから常に最新版を起動できるようにしました。
- デスクトップショートカットをインストール・更新時に自動作成します。
- バージョン別フォルダは最新版とひとつ前の版だけを残し、それ以前を更新時に自動整理します。
- 自動整理はアプリ本体の数値バージョンフォルダだけを対象とし、設定・認証・ダウンロード画像には触れません。

## v0.3.11

- ダウンロード設定を「アカウント → 取得対象 → 取得範囲 → 保存先」の順に整理しました。
- 選択中サービスに合うログインボタンだけを表示し、X Likes専用設定など不要な項目を自動で隠します。
- 重複防止・内部DB・Hitomi互換など普段変更しない項目を「詳細設定」へまとめました。
- pixivブックマークでは意味の異なる日付指定を無効化し、画面上の説明を分かりやすくしました。
- pixivでは同じ動作だった「作品」と「イラスト」を「作品」へ一本化しました。

## v0.3.10

- pixivの「最近のブックマーク」で、作品の投稿日が前回取得より古いと新規ブックマークでも除外される問題を修正しました。
- pixivブックマークの差分取得は、ブックマーク一覧を新しい順に確認し、対象専用archiveと既存ファイルで保存済みを除外します。
- 作品投稿の通常差分取得では、従来どおり投稿日による絞り込みを使用します。

## v0.3.9

- X / pixiv取得中の連続保存通知をまとめ、「最近のダウンロード」一覧とプレビューの再描画回数を大幅に削減しました。
- 大きな画像は表示サイズ付近へ縮小しながら読み込み、取得中のUI停止とメモリ使用量を抑えました。
- 取得開始前の既存ファイル走査をバックグラウンド化し、大きな保存先でも画面操作を止めにくくしました。

## v0.3.8

- pixivが返す `pixiv://account/login` 形式のOAuthコールバックを、QtWebEngineの画面遷移前に捕捉できるようにしました。
- 従来のHTTPSコールバック形式にも引き続き対応します。
- pixiv連携失敗時に、認証情報を伏せた終了段階と診断内容を画面および `pixiv-oauth-last.log` へ残すようにしました。
- 配布版gallery-dlに、アプリからの認証コードを標準入力パイプ経由で受け取る専用モードを追加しました。
- その専用モードがpixiv認証入力を待機できることを、Windowsビルド時に実EXEで検査するようにしました。

## v0.3.7

- 左側のアカウント一覧からXとpixivを切り替えた際も、切替元の作業欄を変更前に正しく保存するようにしました。
- 登録済みアカウントの選択中はサービスをそのアカウントのX / pixivへ固定し、認証と取得サービスの食い違いを防止しました。
- 同じ取得対象を複数アカウントで使う場合も、アカウントごとに異なる保存先を保持します。

## v0.3.6

- アカウントごとに、最後に使ったURL・ユーザー名・ID、取得対象、保存先、取得範囲を記憶するようにしました。
- アカウント切替時に各アカウントの作業欄を自動保存・復元し、初めて選ぶアカウントには別アカウントの値を引き継がないようにしました。
- 従来の取得対象ごとの保存先記憶もそのまま利用できます。

## v0.3.5

- pixivのOAuthコールバックURLに追加パラメータが含まれる場合、誤った値を認証コードとして送信して連携に失敗する問題を修正しました。
- pixivへはURL全体ではなく、解析済みの認証コードだけを安全に渡すようにしました。

## v0.3.4

- Xログインを使いやすくし、新規アカウントの推奨方式を既定にしました。
- X Cookieの削除イベントを反映し、古いログインの再保存を防止しました。
- アカウント一覧に認証保存状態を表示し、ログイン更新への近道を追加しました。
- pixiv OAuthをアプリ内へ統合しました。開発者ツールや認証コード、refresh tokenの手入力は不要です。
- pixiv認証はアカウント別キャッシュへ分離し、accounts.jsonやログへtokenを保存しません。
- X / pixivともログイン保存後に実際の認証テストを行えます。
